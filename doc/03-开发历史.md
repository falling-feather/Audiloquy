# Audiloquy / 语澜 — 开发历史

> **文档梗概**：只记录已经发生的独立 Git 提交，以及完成阶段形成的实际结果。
>
> **具体规划法则**：不以口头完成、文件时间或计划代替 Git 历史；提交标题、任务编号与实际仓库保持一一对应。
>
> **最后一次更新时间**：2026-09-08  
> **更新者**：开发组

---

## 0. 当前版本与记录口径

- **当前项目版本**：未编号原型。2026-09-08 按用户授权初始化 Git，并同步到 [falling-feather/Audiloquy](https://github.com/falling-feather/Audiloquy)；此前迭代仍按工作区记录保留，不补造旧提交。
- **提交标题格式**：本次首次同步按用户直接发布授权使用无版本号的 `类型: 描述（REL-001）`；正式版本策略未分配，不自行编造版本号。后续正式版本提交沿用既定编号规范。
- **项目对接扩展**：未启用；当前没有正式责任组。
- **版本汇总口径**：正式版本与提交只记录真实 Git 历史；未建立 Git 时，可单独记录已验收的工作区功能块及交付证据，不编造版本或提交。

---

## 目录

1. [版本索引](#1-版本索引)
2. [大版本与中版本汇总](#2-大版本与中版本汇总)
3. [独立提交记录](#3-独立提交记录)
4. [提交前记录规则](#4-提交前记录规则)
5. [回滚与历史修正](#5-回滚与历史修正)
6. [关联文档](#6-关联文档)

---

## 1. 版本索引

| 大版本或中版本 | 日期范围 | 实际结果 | 提交记录入口 |
| --- | --- | --- | --- |
| 尚无记录 | — | 尚未建立版本 | — |

## 2. 大版本与中版本汇总

当前没有正式 Git 版本。以下是已实现并验证的工作区迭代记录。

### 2.1 独立备课第一轮

- **日期**：2026-09-08。
- **任务索引**：UI-003、AUDIO-005、DATA-003。
- **实际完成**：从空白管理题组，复制/删除/排序及结构撤销重做；后台音频生成、进度和取消；节奏变化复用语音缓存；草稿原子保存、独立恢复、相对资源解析和工程目录包。
- **关键修正**：每遍朗读后留停顿、静音按整帧并安全分块；单独 Woman 正确识别；结构撤销保留存活题组后续编辑并使旧整套音频失效；生成期间锁定声音设置与打开/保存快捷入口；修正小窗口右栏裁切。
- **验证结论**：模块测试覆盖缓存、真实 SAPI/外置 helper 取消、Qt 线程生命周期、中文路径、替换失败保留旧文件、草稿隔离、另存为和包搬移；GUI 冒烟覆盖题组按钮与自动试听；`prototype_workflow_smoke` 通过真实 UI 保存、重开、生成、保存音频引用、服务打包及搬移后 UI 打开/播放/停止。用例定义见 [CMakeLists.txt](../CMakeLists.txt)。
- **完成凭据 / 交付依据**：最终 Release 构建后 17/17 CTest 全部通过；从干净目录制作 `dist/Audiloquy-win64.zip`（35,476,677 字节），解压后仅保留 Windows 系统 PATH 运行 `--workflow-smoke-test`，退出码 0；发行暂存目录私有文件/模型/用户结果检查为 0。`dist/Audiloquy/Audiloquy.exe` 与暂存包 EXE 一致，原发行目录内已有用户结果未清理。
- **实现入口**：[开发者文档](01-开发者文档.md)、[使用说明](../README.txt)。
- **包含版本 / Git 提交**：暂无，仍为未编号开发原型；两名实现子智能体均指定 Luna / max，主代理负责设计、审查、构建和验收。
- **未验证项**：教师实际课堂试用与神经模型音质不属于本轮验收；恢复已验证服务生命周期，未进行断电测试。

### 2.2 GitHub 源码同步与桌面入口

- **日期 / 任务**：2026-09-08，REL-001。
- **实际完成**：创建公开仓库 `falling-feather/Audiloquy`，同步 `main` 源码，加入 GitHub 首页 README；保留便携包内 README.txt 作为操作说明。
- **完成凭据**：初次推送为 `fe4e3267311f0d2de2bc7f834f72b0cda28115e7`，GitHub API 已确认 README.md 可读取。70 个上传文件排除检查中，禁入路径与真实密钥命中均为 0；原始考试资料、音频、构建产物和私有配置未上传。
- **桌面入口**：本机 `语澜 Audiloquy.lnk` 指向 `dist/Audiloquy/Audiloquy.exe`，图标使用 EXE 内嵌图标，工作目录为项目根，因此未保存工程的结果进入该目录下的 `ListeningStudioResults/`。
- **范围**：本次发布源码仓库；未添加正式版本号、开源许可证或二进制 Release。程序代码与已验证的最新版相同，未因 README 修改重复执行音频测试。
- **关联说明**：[README](../README.md)、[开发者文档](01-开发者文档.md)。

## 3. 独立提交记录

以下为用户直接授权的首次同步提交，尚未进入正式版本编号轨道。

- 2026-09-08：`chore: publish Audiloquy prototype（REL-001）`，提交 [fe4e326](https://github.com/falling-feather/Audiloquy/commit/fe4e3267311f0d2de2bc7f834f72b0cda28115e7)。建立源码基线，加入 README 与发布排除规则。
- 本次记录维护提交：`docs: record GitHub synchronization（REL-001）`，补记远端同步和桌面入口，不改变程序行为。

## 4. 提交前记录规则

1. 由项目负责人先在[02-项目规划.md](02-项目规划.md#10-任务安排)分配目标版本、任务和主责；
2. 一次提交只对应一个版本号和一个任务编号；
3. 提交标题必须与本文件记录完全一致；
4. 独立提交章节只记录已经发生的提交；工作区功能块使用独立迭代记录，说明验收证据与未提交状态；
5. 若提交改变当前实现，同步更新[01-开发者文档.md](01-开发者文档.md)；
6. 若提交完成任务，先写本历史，再由项目规划按周期规则清理完成项。

## 5. 回滚与历史修正

- 回滚保留原提交记录，使用新的 `revert` 提交记录撤销动作；
- 历史文字错误通过新文档任务和提交修正，不删除既有版本、任务和提交的对应关系；
- 在 Git 仓库建立前，不把当前研究文件补写成不存在的历史提交。

## 6. 关联文档

- [00-项目总纲.md](00-项目总纲.md)
- [01-开发者文档.md](01-开发者文档.md)
- [02-项目规划.md](02-项目规划.md)
- [高考基准语料清单](../data/gaokao-corpus-manifest.json)
